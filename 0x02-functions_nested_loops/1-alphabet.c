#include "main.h"
/**
 * main - entry point
 * code: prints alphabet in lowercase, followed by a new line
 * Return: Nothing
 */

int main(void)
{
	char i;

	for (i = 'a'; i = 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
