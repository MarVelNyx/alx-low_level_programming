#include "main.h"
/**
 * _abs - check description
 * @r: input
 * Description: fuction computes absolute value
 * of a value
 * Return: value
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
